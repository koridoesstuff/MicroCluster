# This machine runs Python 3.14. Use >= constraints only, never exact pins:
# exact pins have already caused binary-wheel resolution failures on this
# project. Let pip pick the newest compatible build for the interpreter.

pytest>=8.3

# The simulator's benchmark (simulation/benchmark.py) trains a small
# classifier on simulated ground truth and scores it against the
# hand-authored threshold rules. numpy is scikit-learn's own dependency
# and is also used directly for the feature matrices.
numpy>=2.1
scikit-learn>=1.5

# The interface layer (api/ + web/): a FastAPI service that runs the
# simulation and the engines server-side and serves the plain-JS floor-plan
# animation in web/. No frontend build step.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27   # FastAPI's TestClient (tests/test_api.py) needs it
